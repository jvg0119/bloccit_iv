<h1>All Advertisements</h1>

<% @advertisements.each do |ad| %>
  <%#= link_to ad.title, ad %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to(ad.title, ad) %>
      </h4>
    </div>
  </div>

<% end %>
