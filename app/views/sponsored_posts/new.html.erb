<h1>New Sponsored Post</h1>

  <div class="row">
    <div class="col-sm-4">
      <p>Guidelines for Sponsored Posts</p>
      <ul>
        <li>Make sure it rhymes.</li>
        <li>Don't use the letter A</li>
        <li>The incessant use of hashtags will get you banned.</li>
      </ul>
    </div>


    <div class="col-sm-8">

      <%#= form_for @post, url:  topic_post_path(@post.topic, @post)  do |f| %><!-- long way -->
      <%#= form_for [@sponsored_post.topic, @sponsored_post] do |f| %>
      <%= form_for [@topic, @sponsored_post] do |f| %>
      <%# form_for [@topic, @post] do |f| %>
        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title, class: "form-control", placeholder: "Enter sponsored post title", autofocus: true %>
        </div>

        <div class="form-group">
          <%= f.label :body %>
          <%= f.text_area :body, rows: 8, class: "form-control", placeholder: "Enter sponsored post body" %>
        </div>

        <div class="form-group">
          <%= f.number_field(:price, in: 25..50, step: 1) %>
        </div>

        <div class="form-group">
          <%= f.submit "Save", class: "btn btn-success" %>
        </div>

      <% end %>

    </div>
  </div>
